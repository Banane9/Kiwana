namespace Kiwana.Api
{
    /// <summary>
    /// Enum for the MessageTypes.
    /// </summary>
    public enum MessageTypes
    {
        PRIVMSG,
        NOTICE,
        JOIN,
        PART,
        QUIT,
        MODE,
        PASS,
        NICK,
        USER,
        PONG
    }
}